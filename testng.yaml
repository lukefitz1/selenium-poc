name: Selenium poc
parameters:
  pplatform: Platform.MAC
  pbrowser: chrome
  pversion: "50"
  purl: "http://localhost:4444/wd/hub"
  pos: mac
  pdevice: desktop
  pwidth: 1280
  pheight: 800
  env: "www"
  ppayment_type: "Card"
  purl_prefix: "http://"
  puser: "devops@blueacorn.com"
  ppass: "pass4devops"
listeners:
  - utilities.Listener
tests:
  - name: Http status code test on homepage on chrome for mac
    parameters:
      env: "rmstaging"
      base_urls: 
        - "rebeccaminkoff.com/,uriminkoff.com/"
    classes:
      - tests.HttpStatusCodeTest
  - name: Checkout with test product on chrome for mac
    parameters:
      env: "rmstaging"
      ppayment_type: "Check/Mo"
      base_urls: 
        - "rebeccaminkoff.com/"
      puser: "fet_cf_rm@blueacorn.com"
      ppass: "pass4fet_cf_rm"
    classes:
      - tests.CheckoutTest
  - name: Account login test on chrome for mac 
    parameters:
      env: "rmstaging"
      puser: "fet_lc_rm@blueacorn.com"
      ppass: "pass4fet_lc_rm"
      base_urls: 
        - "rebeccaminkoff.com/,uriminkoff.com/"
    classes:
      - tests.AccountLoginTest
  - name: Header visual test on chrome for mac 
    parameters:
      env: "rmstaging"
      base_urls: 
        - "rebeccaminkoff.com/,uriminkoff.com/"
    classes:
      - tests.HeaderVisualTest
  - name: Account login test HAR on chrome for mac 
    parameters:
      env: "www"
      purl_prefix: "https://"
      puser: "fet_lc_har@blueacorn.com"
      ppass: "pass4fet_lc_har"
      base_urls: 
        - "harney.com/"
    classes:
      - tests.AccountLoginTest